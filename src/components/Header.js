import { AiOutlineCheckSquare } from "react-icons/ai";

const Header = () => {
  return (
    <header>
      <AiOutlineCheckSquare size={30} />
      <h1>Website Todo</h1>
    </header>
  );
};

export default Header;
